<nav>
  <div class="bar">
    <p><span class="dot"></span>FRAU WUNDERWALD</p>
    <div class="hamburger">
      <div class="line"></div>
      <div class="line"></div>
      <div class="line"></div>
    </div>
  </div>
  <div class="content">
    <a
      target="_blank"
      rel="noopener noreferrer"
      href="https://www.ebay-kleinanzeigen.de/pro/Frau-Wunderwald"
      >Kleinanzeigen</a
    >
    <a
      target="_blank"
      rel="noopener noreferrer"
      href="https://frau-wunderwald.de/">Dekoshop</a
    >
    <a
      target="_blank"
      rel="noopener noreferrer"
      href="https://www.instagram.com/frau_wunderwald/">Instagram</a
    >
    <a
      target="_blank"
      rel="noopener noreferrer"
      href="https://frau-wunderwald.de/pages/impressum-frau-wunderwald"
      >Impressum</a
    >
  </div>
</nav>

<script>
  document.querySelector(".hamburger")!.addEventListener("click", () => {
    document.querySelector("nav")!.classList.toggle("expanded");
  });
</script>

<style lang="scss">
  .dot {
    width: 18px;
    height: 18px;
    background-color: #ffff00;
    border-radius: 50%;
    display: inline-block;
    margin-right: 10px;
  }

  nav {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 10;
    display: flex;
    flex-direction: column;
    font-size: 17px;
    overflow: hidden;

    &::after {
      content: "";
      position: absolute;
      width: 100%;
      height: 100vh;
      top: 0;
      left: 0;
      z-index: -1;
      background-color: rgba(255, 255, 255, 0.747);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
    }

    p {
      display: flex;
      align-items: center;
      margin: 0;
    }

    .bar {
      display: flex;
      align-items: center;
      justify-content: space-between;

      p {
        padding: 14px 24px;
      }

      .hamburger {
        cursor: pointer;
        padding: 14px 24px;

        .line {
          transition: transform 0.2s ease-in-out, opacity 0.2s ease-in-out;
          height: 2px;
          width: 18px;
          background: #000;
          margin-bottom: 4px;

          &:nth-child(1) {
            transform-origin: 0% 0%;
          }

          &:nth-child(3) {
            transform-origin: 0% 100%;
          }
        }
      }
    }
  }

  nav.expanded {
    height: 100%;
    .content {
      > * {
        animation: slideIn 0.15s ease-out forwards;
        opacity: 0;
      }

      > *:nth-child(1) {
        animation-delay: 0s;
      }
      > *:nth-child(2) {
        animation-delay: 0.05s;
      }
      > *:nth-child(3) {
        animation-delay: 0.08s;
      }
      > *:nth-child(4) {
        animation-delay: 0.12s;
      }
    }

    @keyframes slideIn {
      0% {
        opacity: 0;
        transform: translateX(150px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .hamburger {
      .line {
        &:nth-child(1) {
          transform: rotate(45deg);
        }

        &:nth-child(2) {
          opacity: 0;
        }

        &:nth-child(3) {
          transform: rotate(-45deg);
        }
      }
    }
  }

  .content {
    display: none;
    display: flex;
    flex-direction: column;
    gap: 30px;
    padding: 32px 24px;

    a {
      font-size: 40px;
      color: #000;
      text-decoration: none;
    }
  }

  nav:not(.expanded) .content {
    display: none;
  }
</style>
